spring:
  application:
    name: Config-prod-profile
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://localhost:8088/eureka
  instance:
    prefer-ip-address: true
