spring:
  application:
    name: USER-SERVICE
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://localhost:8088/eureka
  instance:
    prefer-ip-address: true
